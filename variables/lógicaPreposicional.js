// const logica = !(5 > 6) && !(7 < 3);

const logica = '';

/* 
    Si escribimos una variable sola dentro de una condición, 
    esta variable tiene que tener true o algún valor para dar verdadero.

    false: cuando el valor de la variable sea false, undefined, NaN, null o string vacío.
    aclaración: Los arrays y objetos vacíos cuentan como true.
*/

if (logica) {
    console.log('si');
} else {
    console.log('no');
}